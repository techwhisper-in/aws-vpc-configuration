pipeline:
  identifier: Build_aws_vpc_configuration_1720103295039
  name: Build aws-vpc-configuration
  orgIdentifier: default
  projectIdentifier: myfirstproject
  properties:
    ci:
      codebase:
        build: <+input>
        connectorRef: connect_to_techwhisperin
        repoName: aws-vpc-configuration
  stages:
    - stage:
        identifier: build
        name: build
        spec:
          caching:
            enabled: true
          cloneCodebase: true
          execution:
            steps:
              - step:
                  identifier: echo
                  name: echo
                  spec:
                    command: echo hello world
                  timeout: ""
                  type: Run
          platform:
            arch: Amd64
            os: Linux
          runtime:
            spec: {}
            type: Docker
        type: CI
